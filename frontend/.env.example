# API URL configuration for production deployment (Render backend)
# In local development, you can leave this blank or comment it out; the frontend will fallback to http://localhost:5000/api
VITE_API_URL=http://localhost:5000/api
